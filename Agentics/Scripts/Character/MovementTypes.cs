using UnityEngine;

namespace Agentics
{
    public enum MoveType
    {
        Idle,
        Walking,
        Running
    }

    public enum Direction
    {
        Right,
        Left,
        Up,
        Down,
        None
    }
}